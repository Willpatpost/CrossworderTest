// main.js
import { CrosswordSolver } from './CrosswordSolver.js';

document.addEventListener('DOMContentLoaded', () => {
  // Instantiate and initialize the solver
  const crosswordSolver = new CrosswordSolver();
  crosswordSolver.init();
});
